import React from 'react';
import { View, Text } from 'react-native';
import { styles } from './styles';

const AccountScreen = () => (
	<View>
		<Text style={styles.container}>Account Screen</Text>
	</View>
);

export default AccountScreen;

import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-elements';

const Home = () => {
	return (
		<View>
			<Text h4>Home</Text>
		</View>
	);
};

export default Home;
